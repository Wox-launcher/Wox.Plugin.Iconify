[project]
name = "iconify"
version = "0.0.6"
description = "Search iconify icons - Home of open source icon sets"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["wox-plugin==0.0.70"]

[project.optional-dependencies]
dev = ["ruff", "mypy"]

[tool.ruff]
line-length = 140
